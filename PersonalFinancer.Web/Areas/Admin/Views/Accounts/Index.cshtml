@using PersonalFinancer.Common.Constants;
@using PersonalFinancer.Web.Models.Account;
@model UsersAccountsCardsViewModel
@{
	ViewData["Title"] = "All Accounts";
}

<div class="container">

	<h1 class="display-6 text-center">@ViewData["Title"]</h1>

	@*Account Cards*@
	<div class="row mt-4" id="accounts">
		@foreach (var account in Model.AccountsCards)
		{
			<div class="col-sm-6 col-lg-4 col-xl-3">
				<div class="card align-items-center shadow mb-4">
					<div class="d-flex align-items-center">
						<div class="p-2">
							<img src="/icons/icons8-wallet-64.png" style="width: 50px;">
						</div>
						<div class="card-body p-2 flex-grow-1">
							<h5 class="card-title display-6 fs-5"><b>@account.Name</b></h5>
							<p class="card-text display-6 fs-6">@account.Balance @account.CurrencyName</p>
						</div>
					</div>
					<div class="d-flex">
						<a asp-area="Admin" asp-controller="Accounts" asp-action="AccountDetails" asp-route-id="@account.Id" class="btn btn-secondary m-2">Details</a>
						<a asp-area="Admin" asp-controller="Users" asp-action="Details" asp-route-id="@account.OwnerId" class="btn btn-secondary m-2">Owner</a>
					</div>
				</div>
			</div>
		}
	</div>

	@*Pagination*@
	<partial name="_PaginationPartial" />
</div>

@section Scripts {
	<script>
		let params = {
			url: '@UrlPathConstants.ApiAccountsEndpoint',
			pages: @Model.Pagination.Pages
																		}
	</script>
	<script src="~/js/getAccounts.js"></script>
	<script src="~/js/setUpPagination.js"></script>
}